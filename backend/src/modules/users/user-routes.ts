import { Router } from "express";

// User CRUD (create, list, activate/deactivate) is implemented in
// Phase 7 — User Module. Placeholder only — see auth-routes.ts for why.
export const userRoutes = Router();
