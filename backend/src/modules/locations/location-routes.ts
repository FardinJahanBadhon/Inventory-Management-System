import { Router } from "express";

// Location CRUD (create, list, activate/deactivate) is implemented in
// Phase 6 — Location Module. Placeholder only — see auth-routes.ts for why.
export const locationRoutes = Router();
