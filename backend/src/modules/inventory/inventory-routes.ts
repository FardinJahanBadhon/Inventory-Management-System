import { Router } from "express";

// Listing, Initialization, Distribution, and Trash are implemented in
// Phase 9 — Inventory Module. Placeholder only — see auth-routes.ts for why.
export const inventoryRoutes = Router();
