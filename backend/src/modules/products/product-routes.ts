import { Router } from "express";

// Product CRUD (create, list, deactivate) is implemented in
// Phase 8 — Product Module. Placeholder only — see auth-routes.ts for why.
export const productRoutes = Router();
